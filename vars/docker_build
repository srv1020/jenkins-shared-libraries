def call(String project_name, String image_tag, String User_Name ){
  echo "this is building code"
  sh "docker build -t ${User_name}/${project_name}:${image_tag} ."
  echo "code building successful"
}
